MYSQL_DATABASE=portfolio
MYSQL_USER=portfolio
MYSQL_PASSWORD=changeme
MYSQL_ROOT_PASSWORD=changeme_root

# Backend
SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/portfolio
SPRING_DATASOURCE_USERNAME=portfolio
SPRING_DATASOURCE_PASSWORD=changeme

# Finnhub (free tier) — live quote/profile/news proxy. Get a key at https://finnhub.io
FINNHUB_API_KEY=

# Comma-separated origins allowed to call the API (CORS)
CORS_ALLOWED_ORIGINS=http://localhost:5173

# AI (only needed if the chatbot module is enabled — out of scope for now)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
